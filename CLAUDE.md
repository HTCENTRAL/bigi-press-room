# BIGI PRESS ROOM — プロジェクト固有指示

## プロジェクト概要
Google Apps Script (GAS) によるアパレルサンプル貸出管理システム。
開発者: ひとし / 顧客（システム利用者）: ららん（アナログ操作、GAS知識なし）。
Git: https://github.com/HTCENTRAL/bigi-press-room.git (master)

## ファイル構成
- `Code.gs` — メインロジック（onOpen, setupSheets, registerLoan, processReturn, processPublish, generateFormattedSheet, revertToUnreturned, getStaffList）
- `Sidebar.html` — 新規貸出登録フォーム（サイドバー）
- `ReturnDialog.html` — 返却処理ダイアログ（2ステップ: 返却登録 → 掲載登録）
- `MediaSetDialog.html` — 伝票編集ダイアログ（基本情報・媒体セットCRUD・未返却に戻す）
- `FormattedSheetDialog.html` — 整形シート生成ダイアログ
- `PublishListEditDialog.html` — 掲載リストマスタ編集ダイアログ
- `tasks/todo.md` — 作業チェックリスト
- `tasks/changelog.md` — セッション別実装記録
- `tasks/lessons.md` — 教訓・注意事項

## スプレッドシート列定義（変更禁止・厳守）

### 貸出管理シート（A〜U列）
| 列 | 内容 | 備考 |
|---|---|---|
| A | 伝票番号 | 5桁ゼロ埋め、setNumberFormat('@') |
| B | 貸出日 | |
| C | スタイリスト名 | |
| D | 電話番号 | setNumberFormat('@') |
| E | 媒体名 | パイプ区切り（' \| '） |
| F | 月号 | パイプ区切り |
| G | テーマ/着用者 | パイプ区切り |
| H | 公開日 | パイプ区切り |
| I | 種別 | パイプ区切り（雑誌(紙面)/雑誌(WEB)/TV/その他） |
| J | 撮影日 | |
| K | 返却予定日 | |
| L | ブランド | |
| M | 品番 | setNumberFormat('@') |
| N | 色番 | setNumberFormat('@') |
| O | アイテム名 | |
| P | 単価 | |
| Q | 返却ステータス | 未返却/返却済 の2択のみ |
| R | 返却日 | |
| S | 掲載済 | チェックボックス |
| T | 備考 | |
| U | 担当者名 | |

### 掲載リストマスタシート（A〜O列）
| 列 | 内容 |
|---|---|
| A | 伝票番号 |
| B | 雑誌名 |
| C | 種別 |
| D | 掲載号 |
| E | 公開日 |
| F | テーマ/着用者 |
| G | スタイリスト |
| H | ブランド名 |
| I | 品番 |
| J | 色番 |
| K | 上代 |
| L | アイテム |
| M | 頁 |
| N | 画像/リンク |
| O | 担当者名 |

### 設定シート
- A1〜: 月末配信先メールアドレス
- A3: 「ブランド名」ラベル / B3: ブランド名の値

### 担当者マスタシート
- A列: 担当者名リスト（1行目ヘッダー）

## 重要な設計原則（破ってはいけない）

### 1行=1アイテム
- アイテムが2件あれば同一伝票番号で2行生成される（重複ではない）
- 「重複レコード」報告を受けたら、まず伝票番号とアイテム数を確認する

### パイプ区切り（' | '）
- 媒体セット（E/F/G/H/I列）は ' | ' で区切る
- カンマは媒体名等に含まれうるため使用禁止

### 返却ステータスはMECE
- Q列: 「未返却」「返却済」の2択のみ
- 掲載済はS列チェックボックスで独立管理（ステータスに混ぜない）

### 条件付き書式
- 未返却=赤(#ffcccc) のみ。式: `=$Q2="未返却"`

### 伝票番号
- 08877の続きから自動採番（5桁ゼロ埋め）
- `getLastDataRow()`: createTextFinderでA列検索（チェックボックス影響を受けない）

## 返却・掲載処理フロー
1. **返却登録**: 伝票番号 → アイテム一覧（全チェックデフォルト） → `processReturn({returnDate, rows:[{rowIndex}]})`
2. **掲載登録**: 掲載チェックアイテム × 媒体セット展開 → 選択 → `processPublish({combinations:[...]})`
   - S列チェック=true + 掲載リストに1組み合わせ1行転記

## 整形シート生成
- シート名: `整形用_{Y}年{M}月`（既存は上書き）
- TYPE_ORDER: 雑誌(紙面) → 雑誌(WEB) → TV → その他
- 各グループ: オレンジ(#FF9900)タイトル行 + 薄オレンジ(#FFD9B3)ヘッダー行
- 公開日昇順、媒体変わり目に空白行、列単位重複除去

## 注意事項
- コミット前に対象ファイルをユーザーに明示して確認を取る
- コード非公開化（GAS Library化）は全機能完成後に実施予定（現状は現状維持）
- GASはブラウザ上で動作するため、ローカルでのテスト実行は不可
